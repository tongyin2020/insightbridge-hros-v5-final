# InsightBridge HROS V5 — 代码审查问题汇总

**两轮审查，共发现 69 个问题，全部处理完毕**  
**最终状态：7/7 测试通过 ✅**

---

## 第一轮（主系统文件）：32 个问题

| 优先级 | 已修复问题 |
|--------|-----------|
| 🔴 Critical | `re` 未 import（NameError崩溃） |
| 🔴 Critical | `DirectLTVDecision` 重复定义冲突（AttributeError） |
| 🔴 Critical | HROS V5 函数定义后从未被调用 |
| 🔴 Critical | V5 函数指向旧 V4 目录路径 |
| 🔴 Critical | `assert best is not None` 被 `-O` 禁用 |
| 🔴 Critical | `float("High")` 崩溃（model_quality.py） |
| 🔴 High | `demand_state` 永远是 "NORMAL"（三系统均修复） |
| ⚠️ Medium | 冗余 sys 别名、MakCorps 注释、425→76家、NORMAL注释 |

## 第二轮（支撑模块）：37 个问题

| 优先级 | 已修复问题 |
|--------|-----------|
| 🔴 Critical | `fair_pricing.py` — None 导致 TypeError 崩溃 |
| 🔴 Critical | OTA节奏：短错误页（500字符）被缓存为真实信号 |
| 🔴 High | `real_data.py` — 重复 key 覆盖珠海 Firecrawl 来源 |
| 🔴 High | BrightData token 硬编码在源码中（安全风险） |
| 🔴 High | `hotel["en"]` KeyError — 改为 `.get()` |
| 🔴 High | integration_adapters.py — ValueError 未捕获 |
| ⚠️ Medium | url_23→url_3 命名修正、酒店数量注释修正 |
| ⚠️ Medium | 降级无警告 → 添加 logging.warning |
| ⚠️ Medium | direct_price=0/base_price=0 静默影响决策 |

---

## 数据因子状态

| 状态 | 因子 | 来源 |
|------|------|------|
| ✅ | DSEC ADR、天气、渡轮 | 本地/免费API |
| ✅ | 活动密度、口岸客流、珠海溢出 | Firecrawl (5130额度) |
| ✅ | Booking.com 市场价 | Firecrawl |
| ✅ | Booking.com 每家酒店 | BrightData ($204余额) |
| ✅ | 官网 BAR 价格 | Playwright + BrightData兜底 |
| ⚠️ | Agoda 每家酒店 | 待 BrightData 配置 |
| ❌ | MakCorps | 已停用（设计如此） |

---

*审查时间：2026-06-05 | InsightBridge Global | Claude Sonnet 4.6*
